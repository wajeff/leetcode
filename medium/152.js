/**
 * @param {number[]} nums
 * @return {number}
 */
var maxProduct = function(nums) {
    let left = 0
    let right = nums.length - 1
    let count = 0
    while(left < nums.length){
        
    }
};
maxProduct([6,3,-10,0,2])